# The dev profile is the only profile allowed to opt into demo seed migrations.
# The dev-only seed location is added in the dedicated seed implementation.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:80}
app.cloudinary.upload-folder=shopping-website/dev
